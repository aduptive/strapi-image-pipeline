{
  "name": "image-pipeline-development",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "pack:local": "node scripts/pack.mjs",
    "check": "npm test && npm run pack:local && node scripts/check-pack.mjs",
    "audit:packages": "node scripts/audit-packages.mjs",
    "check:install": "node scripts/check-install.mjs"
  },
  "devDependencies": {
    "@strapi/utils": "5.52.1",
    "dompurify": "3.4.15",
    "esbuild": "0.25.12",
    "jsdom": "24.1.3",
    "sharp": "0.35.4",
    "svgo": "4.1.0"
  }
}
